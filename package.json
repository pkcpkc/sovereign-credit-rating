{
  "name": "sovereign-credit-rating-wiki",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync": "mm sync .",
    "sync:verbose": "mm sync . --verbose",
    "sync:pr": "mm sync . --pr",
    "resync": "mm resync .",
    "resync:verbose": "mm resync . --verbose",
    "overviews": "mm overviews .",
    "publish": "mm publish . ./dist/site",
    "serve": "mm serve .",
    "serve:dist": "mm serve ./dist/site",
    "check": "mm check-plugins",
    "test": "vitest run",
    "link:local": "npm install ../mycelium-mind --no-save",
    "link:npm": "npm install"
  },
  "dependencies": {
    "mycelium-mind": "^0.2.4"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}