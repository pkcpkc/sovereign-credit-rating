$meta:
  type: Schema
  title: "Baseline Summary Schema"
  description: "Defines the core system metadata fields extracted into summaries."

type: "Summary" # String | Required | Must be exactly "Summary".
title: string # String | Required | Sanitized title for the source note.
tags: [string] # Array | Required | Standard categorization tags.
assets: [string] # Array | Required | File paths to companion raw assets.
